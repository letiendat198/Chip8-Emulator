#ifndef UTILITY 
#define UTILITY
class Utility{
    public:
        int mattoa(int x, int y, int col); 
        int* atomat(int n, int col);
        int* pixtoscreen(int *a);
        short keymap(const char* key);
};
#endif